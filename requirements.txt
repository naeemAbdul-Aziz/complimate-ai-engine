# Core AI Engine Dependencies
llama-index == 0.12.41
openai == 1.75.0
chromadb == 1.0.5
llama-index-retrievers-bm25 == 0.5.2
llama-index-llms-openai == 0.3.37
llama-index-embeddings-openai == 0.3.1

# File Processing
PyPDF2 == 3.0.1
fpdf == 1.7.2

# API Framework
fastapi[standard] == 0.115.9
uvicorn == 0.34.2
python-dotenv == 1.0.0

# Enhanced Logging with Colors
colorlog == 6.8.2

# Testing Dependencies
pytest == 8.0.0
pytest-asyncio == 0.23.0
pytest-cov == 4.1.0
httpx == 0.28.1

# Development Dependencies  
black == 24.3.0
isort == 5.13.0
flake8 == 7.0.0

# Vector storage and enhanced features
chromadb == 0.4.15  # Persistent vector storage
# sqlalchemy == 2.0.23  # Database ORM for metadata (temporarily disabled due to Python 3.13 compatibility)
# alembic == 1.12.1  # Database migrations (temporarily disabled due to Python 3.13 compatibility)

# Additional Utilities (already included in dependencies above, but listing for clarity)
# pydantic - included with FastAPI
# typing-extensions - included with FastAPI
# pathlib - standard library